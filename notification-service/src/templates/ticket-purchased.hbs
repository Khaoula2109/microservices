<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Votre Ticket de Transport</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #102a43 0%, #243b53 100%);
        }
        .container {
            width: 90%;
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }
        .header {
            background: linear-gradient(135deg, #102a43 0%, #243b53 100%);
            color: #ffffff;
            padding: 40px 30px;
            text-align: center;
            position: relative;
        }
        .header::after {
            content: '';
            position: absolute;
            bottom: -20px;
            left: 0;
            right: 0;
            height: 40px;
            background: #ffffff;
            border-radius: 50% 50% 0 0;
        }
        .logo {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
            letter-spacing: 1px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }
        .logo-accent {
            color: #D4A017;
        }
        .header h1 {
            margin: 10px 0 0 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 40px 35px 30px;
            line-height: 1.8;
            color: #333;
        }
        .content p { margin: 0 0 16px 0; }
        .details-box {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            border-left: 4px solid #D4A017;
            padding: 25px;
            border-radius: 8px;
            margin: 25px 0;
        }
        .details-box strong {
            color: #102a43;
            font-weight: 600;
            display: inline-block;
            min-width: 150px;
        }
        .value {
            color: #333;
            font-weight: 500;
        }
        .details-box hr {
            border: none;
            border-top: 1px solid rgba(212, 160, 23, 0.3);
            margin: 15px 0;
        }
        .qr-code {
            background: #ffffff;
            padding: 15px;
            border-radius: 8px;
            margin-top: 10px;
            text-align: center;
            font-family: monospace;
            color: #102a43;
            border: 2px dashed #D4A017;
        }
        .qr-code-image {
            background: #ffffff;
            padding: 20px;
            border-radius: 12px;
            margin: 20px auto;
            text-align: center;
            border: 3px solid #D4A017;
            box-shadow: 0 4px 15px rgba(212, 160, 23, 0.3);
            max-width: 350px;
        }
        .qr-code-image img {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto;
        }
        .qr-code-title {
            color: #102a43;
            font-weight: 600;
            font-size: 16px;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .scan-instruction {
            color: #666;
            font-size: 14px;
            margin-top: 15px;
            font-style: italic;
        }
        .footer {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: #666;
            padding: 25px 30px;
            font-size: 0.85em;
            text-align: center;
            border-top: 3px solid #D4A017;
        }
        .footer-logo {
            font-weight: bold;
            color: #102a43;
            font-size: 1.1em;
            margin-bottom: 8px;
        }
        .footer-accent {
            color: #D4A017;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">Kowihan<span class="logo-accent">Transit</span></div>
            <h1>{{subject}}</h1>
        </div>
        <div class="content">
            <p>Bonjour,</p>
            <p>Merci pour votre achat. Voici les détails de votre ticket.</p>

            <div class="details-box">
                <strong>Type de Ticket :</strong>
                <span class="value">{{ticketType}}</span>
                <hr>
                <strong>ID du Ticket :</strong>
                <span class="value">{{ticketId}}</span>
                <hr>
                <strong>Date d'achat :</strong>
                <span class="value">{{purchaseDate}}</span>
            </div>

            {{#if qrCodeImage}}
            <div class="qr-code-image">
                <div class="qr-code-title">📱 Votre QR Code</div>
                <img src="data:image/png;base64,{{qrCodeImage}}" alt="QR Code du Ticket" />
                <div class="scan-instruction">Présentez ce code au contrôleur lors de votre voyage</div>
            </div>
            {{else}}
            <div class="details-box" style="margin-top: 20px;">
                <strong>Code QR (Données) :</strong>
                <div class="qr-code">{{qrCodeData}}</div>
            </div>
            {{/if}}

            <p style="margin-top: 25px;">Bon voyage avec KowihanTransit ! 🚌</p>
            <p>Cordialement,<br><strong>L'équipe KowihanTransit</strong></p>
        </div>
        <div class="footer">
            <div class="footer-logo">Kowihan<span class="footer-accent">Transit</span></div>
            Conservez ce ticket jusqu'à la fin de votre trajet.
        </div>
    </div>
</body>
</html>