apiVersion: v1
kind: Service
metadata:
  name: postgresql-gis
  namespace: transport-databases
spec:
  selector:
    app: postgresql-gis
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql-gis
  namespace: transport-databases
spec:
  serviceName: postgresql-gis
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-gis
  template:
    metadata:
      labels:
        app: postgresql-gis
    spec:
      containers:
      - name: postgresql-gis
        image: postgis/postgis:16-3.4-alpine
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: db-config
              key: postgres-gis-db
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: db-config
              key: postgres-gis-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: postgres-gis-password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-gis-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - transport_user
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - transport_user
          initialDelaySeconds: 5
          periodSeconds: 5
  volumeClaimTemplates:
  - metadata:
      name: postgres-gis-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
